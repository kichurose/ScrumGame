export class UserInfo {
  public username: string;
  public storypoints: number;
  constructor(username: string, storypoints: number) {
    this.username = username;
    this.storypoints = storypoints;
    
  }
}